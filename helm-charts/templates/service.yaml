apiVersion: v1
kind: Service
metadata:
  name: {{ include "zk-slack.fullname" . }}
  labels:
    {{- include "zk-slack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zk-slack.selectorLabels" . | nindent 4 }}
